# Upstream reference project (SuperbWarfare) — pulled separately, not part of this repo.
# Still used on disk via includeBuild() in settings.gradle.kts.
/SuperbWarfare/
/SuperbWarfare.broken-backup/

# Gradle
.gradle/
build/
out/
!gradle/wrapper/gradle-wrapper.jar

# Generated resources (data generators)
/src/generated/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Run/test client
run/
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Local overrides
local.properties

# Working notes for the phase currently in progress — never committed, kept as a
# personal reference alongside PLAN.md, deliberately out of git history.
PHASE5_PLAN.md
