# План: NPC-отряды для SuperbWarfare (NeoForge 1.21.1)

Статус: ✅ сделано · 🚧 в работе · ⬜ не начато · ⚠️ отклонение от исходного плана

## Фаза 0 — Фундамент аддона ✅

- ✅ Новый Gradle-модуль (NeoForge 1.21.1, Java 21, Kotlin). Зависимость от SuperbWarfare
  через `includeBuild` composite build против локального снапшота (не через опубликованный
  джарник — нужны внутренние API).
- ✅ Базовые реестры: `ModEntities`, `ModItems`, точка входа `SquadMod`.

## Фаза 1 — NPC-сущность и визуал ✅ (модель — ⚠️)

- ✅ `NpcEntity : PathfinderMob`.
- ✅ Не деспавнится при уходе игрока (`removeWhenFarAway() = false`).
- ✅ **Модель: вместо Simple Bedrock Model взяли ванильный `HumanoidModel`.**
  `NpcModel : HumanoidModel<NpcEntity>` + `NpcRenderer : HumanoidMobRenderer`. Причина:
  кастомный SBM+MAE рендер потребовал много итераций на позиционирование оружия в руке,
  а `HumanoidModel` даёт держание оружия (`ItemInHandLayer` + миксины SBW), позы рук
  (`PoseTool` → `ArmPose`), броню и ванильные анимации почти бесплатно. Строки про
  `.geo.json`/`.animation.json`/`BedrockAttachmentModel` из исходного плана неактуальны.
- ✅ ~~Скин — обычная текстура `textures/entity/npc.png`~~ — заменено на 8 скинов по фракциям,
  см. «Фракции вместо цвета» ниже.
- ✅ **Роли** (класс + опытность) — data-driven, делаем сейчас.

## Фаза 2 — Боевой AI 🚧

- ✅ `NpcGunAttackGoal` на базе `GunShootGoal` из SBW (прицеливание/перезарядка/режимы огня),
  но без завязки на систему случайного лута оружия у диких мобов.
- ✅ Ближний бой при нехватке патронов — `MeleeAttackGoal` fallback.
- ✅ Отрядная осведомлённость в таргете: `NpcEntity.isEnemy()` уже фильтрует через
  `SquadTeams.isHostile()` — таргетинг свой-чужой по фракции работает.
- ⬜ Укрытия / подавление / мораль — самая объёмная часть, не начато.
- ⬜ Слух / тревога отряда.

## Фаза 3 — Отряды, цвет, приказы 🚧 (в основном сделано)

Мультитул из Garry's Mod: **один предмет, два режима** (Shift+ПКМ по воздуху). Иконка
временно из SBW (`tactical_terminal`).

- ✅ Цвет команды = vanilla `PlayerTeam` (`SquadTeams`); свой-чужой; турели/техника SBW
  подхватывают. Разные цвета — враги, оба без цвета — нейтрал.
- ✅ **Recruit-режим**: `ПКМ по воздуху` — GUI (класс/ранг/цвет, кнопки-циклы);
  `ПКМ по блоку` — деплой. Конфиг в NBT тула, дефолт — REGULAR / RIFLEMAN / RED.
- ✅ **Command-режим**: `ПКМ по воздуху` — всегда Command GUI. `ПКМ по NPC` — выделить
  (Shift — сбросить выборку). `ПКМ по враждебному` (отряд выбран через [Focus]) — ATTACK
  охотится за целью, DEFEND защищает её. Точка-цель — через кнопку [Objective] в GUI
  (одноразовый прицел, следующий `ПКМ воздух` — раскаст ~220 бл).
- ✅ Выборка не смешивает разные команды (пишет «clear selection or form the squad»).
- ✅ `SquadManager : SavedData` (per-server): id, цвет, участники, приказ, точка-цель,
  focus-сущность. Персист. Смерть NPC → выбывает из отряда, пустые отряды удаляются.
- ✅ Приказы (`SquadOrderGoal` + `SquadFocusTargetGoal`, ниже боевого приоритета):
  DEFEND (держать ±8 от home), PATROL (±12), ATTACK (идти к home), FREE (без коорд.).
- ✅ DEFEND-поводок: не преследует врага дальше ~14 бл от home, полностью отцепляется
  за ~24 и возвращается.
- ✅ Переименование отряда (`RenameScreen`). Командир — убран за ненадобностью.
- ✅ Цвета сокращены до 8.
- ✅ ~~Точка сбора = «казарма» (периодический респаун)~~ — реализовано в Фазе 5.5.
- ⬜ Waypoints для патруля — в работе, см. Фазу 5.6.

## Фаза 4 — Миномёт и гранаты 🚧 (вторая итерация, автонаводка ещё не протестирована)

- ✅ `GrenadeThrowGoal`: GRENADIER докидывает гранату (доп. к M79) на дистанции 5-16 блоков
  на кулдауне ~5-8с. Скорость/гравитация броска — placeholder-константы, нужна калибровка
  по факту в игре. Пользователь подтвердил, что наводится/стреляет из M79 хорошо; сам бросок
  гранаты пока не перепроверен на его стороне.
- ✅ `MortarOperatorGoal` (переписан): класс MORTAR_OPERATOR (пистолет для самообороны) ищет
  ближайший незанятый `MortarEntity` в радиусе **30** блоков (было 40) (`MortarClaims` —
  реестр занятости отдельно для оператора и заряжающего), идёт к нему, наводит через
  `FiringParametersItem.Parameters` + `setTarget` и стреляет `vehicleShoot`.
  Приоритет цели: если приказ ATTACK — фокус/точка отряда; иначе (и вообще без приказа) —
  **автономно сканирует врагов** в радиусе обнаружения. Радиус обнаружения зависит от ранга:
  80 (RECRUIT) → 160 (ELITE) блоков, линейная интерполяция по рангу.
- ✅ `MortarLoaderGoal` (новое): класс MORTAR_LOADER идёт к тому же/ближайшему незанятому
  миномёту (свой реестр занятости, независимый от оператора) и раз в ~3с проверяет боезапас;
  если снарядов < 4 — досыпает стак `MORTAR_SHELL` (16 шт) через `setItem`. Упрощённая
  логистика без переносимого личного боекомплекта — выбор пользователя вместо «реалистичного»
  варианта.
  **Требует, чтобы миномёт был физически размещён (не обязательно уже заряжен —
  заряжающий сам восполняет)**. Мин. дистанция стрельбы ~15 бл, safe-радиус ~10 бл от своих —
  эвристики, не точное отражение внутреннего решателя миномёта.
- ✅ **Пресеты отрядов** (`SquadPreset`) — деплой тулом теперь может ставить в шеренгу целый
  заранее собранный состав одним кликом (цвет/ранг выбираются как раньше, состав — из пресета):
  SINGLE (как раньше, один NPC любого класса), FOUR (4 автоматчика), SIX (4 авт + 2 снайпера),
  EIGHT (4 авт + 2 снайпера + 1 гренадир + 1 пулемётчик), SIXTEEN (10 авт + 2 снайпера +
  2 пулемётчика + 2 гренадира), MORTAR_CREW (1 наводчик + 1 заряжающий). При пресете ≠ SINGLE
  выбор класса в GUI недоступен (класс приходит из пресета).
- ✅ **Классовые множители дальности/точности** (`NpcClass`, поверх базовых 24 бл и spread ранга):
  дальность — автоматчик ×2, пулемётчик/гренадир ×1.5, снайпер ×3; точность — только снайпер
  дополнительно ×1.2 (по решению пользователя — остальные классы без бонуса точности).
- ✅ **Фикс UX**: выбор фокус-цели (arm focus из Command GUI) теперь применяется к ЛЮБОЙ
  сущности по клику, включая собственных NPC — раньше клик по своему NPC ошибочно попадал в
  ветку обычного selection вместо назначения его целью (`SquadSelection.awaitingFocus`
  проверяется первым в `interactLivingEntity`).
- ✅ **Фикс критического бага друг/враг**: `SquadTeams.isHostile` считала безцветного игрока
  (у игрока никогда не было команды) враждебным ЛЮБОМУ цветному отряду. Раньше это маскировалось
  малой дистанцией стрельбы обычных бойцов, но с автонаводкой миномёта (радиус 80-160 бл без
  приказа ATTACK) стало явным: миномётчики стреляли по игроку, воспринимая это как «стреляют в
  рандомное место»/«гасят всех», и не отпускали пост у миномёта, т.к. цель (игрок) была видна
  почти всегда. Теперь враждебность требует, чтобы У ОБЕИХ сторон был реально назначенный цвет —
  безцветное (включая игрока) всегда нейтрально и никогда не выбирается автономной целью.
- ✅ Друг/враг фикс подтверждён пользователем частично: стрельба по игроку прекратилась, НО
  осталась вторая, отдельная причина «бессмысленной» стрельбы (см. ниже) — пользователь
  перепроверил и увидел, что состав продолжал стрелять без остановки на 400+ блоков (макс.
  дистанция миномёта), пока не появились реальные враги.
- ✅ **Фикс второй причины — стрельба без проверки, попадёт ли снаряд**: `MortarEntity.setTarget`
  молча проваливается (оставляет старое/дефолтное наведение), если для точки нет баллистического
  решения (не хватает дальности/скорости) или угол выходит за пределы `turretMinPitch/MaxPitch` —
  а наш код раньше вызывал `vehicleShoot` каждый тик независимо от результата `setTarget`,
  поэтому расчёт стрелял на дефолтном/устаревшем наведении в максимальную дальность снова и снова.
  `MortarOperatorGoal.canHitTarget()` теперь сам повторяет тот же расчёт (`TrajectoryCalculator` +
  `turretMinPitch/MaxPitch`), что и решатель миномёта, и просто не стреляет в этот тик, если
  решения не существует, вместо слепой стрельбы по устаревшему наведению.
- ✅ **Свой кулдаун стрельбы**: раньше вызывали `vehicleShoot` каждый тик (эффективно стреляли
  на внутреннем кулдауне миномёта ~25 тиков/1.25с); теперь добавлен собственный `nextFireTick`,
  требующий 50 тиков (2.5с) между выстрелами — вдвое медленнее по прямому запросу пользователя.
- ✅ **Разброс попаданий по рангу** (перенастроен по фидбеку — было слишком точно): у SBW уже
  есть встроенный механизм разброса точки падения снаряда — `FiringParametersItem.Parameters.radius`
  (в блоках), который `ArtilleryEntity` использует как `targetPos.center.randomPos(radius)`.
  `MortarOperatorGoal.scatterRadius()` считает его по рангу линейной интерполяцией —
  10 бл (RECRUIT) → 5 бл (ELITE).
- ✅ Подтверждено частично: наводчик перестал стрелять без цели, но
  **заряжающий продолжал стрелять сам по себе**, даже когда наводчик простаивал — третья,
  отдельная причина.
- ✅ **Фикс третьей причины — авто-выстрел при пополнении боезапаса**: у SBW немодифицированный
  ("не intelligent") `MortarEntity.setChanged()` авто-стреляет при ЛЮБОМ изменении инвентаря
  (`vehicleShoot(null, "Main", targetPos.center)`) — так задуман «глупый» миномёт: бросил снаряд
  в казённик — он тут же выстрелил по текущему наведению. Именно это вызывал `MortarLoaderGoal`
  каждым `setItem()` при пополнении — неконтролируемый выстрел мимо всех наших проверок
  (`canHitTarget`, кулдаун, друг/враг). Теперь при захвате миномёта (`MortarOperatorGoal` и
  `MortarLoaderGoal`, оба, на случай кто первый доберётся) выставляется `mortar.intelligent = true`
  — тот же флаг, что выставляет игрок биндом `MonitorItem`, отключающий авто-выстрел; стрельбу
  полностью ведёт `MortarOperatorGoal`. Побочный эффект: при уничтожении миномёта в “intelligent”
  режиме SBW дополнительно роняет `Monitor`-предмет (как будто он был установлен) — минорная
  и не проблемная деталь для геймплея этого аддона.
- ✅ **Подтверждено пользователем в игре: миномётный расчёт работает как задумано** (наводчик
  стреляет только по реальным целям в радиусе, заряжающий не вызывает случайных выстрелов).
- ✅ **Побочная чистка**: у слота миномёта на самом деле ёмкость 1 снаряд (не стопка), а
  `MortarShellItem.stacksTo(8)` — наш `MortarLoaderGoal` пытался класть стек по 16, SBW тихо
  клэмпил это до 1 через `setItem`, но при каждом клэмпе писал warning в лог, а проверка
  `count < 4` из-за лимита в 1 была всегда истинна — заряжающий переустанавливал 1 снаряд на
  1 каждые 3с без необходимости. Теперь кладёт ровно 1 и только когда слот реально пуст.
- ✅ **Подтверждено пользователем: пресеты работают чётко** (деплой шеренгой всех составов).
- ✅ Минимальная дистанция стрельбы миномёта увеличена с 15 до **25 блоков** по фидбеку.
- ⬜ Бросок гранаты гренадиром — пользователь перепроверит сам позже, не трогаю вслепую.

**Фаза 4 закрыта как рабочая.**

## Фаза 4.5 — Быстрое командование (HUD) ✅ (подтверждено пользователем в игре)

Идея «трубы командира» (зум + два экрана поверх игры) заменена по предложению пользователя на
принципиально другой интерфейс — **не блокирующий управление HUD**, открываемый клавишей, без
инструмента в руке.

- ✅ **Лимит отрядов**: `SquadManager.MAX_SQUADS_PER_OWNER = 9` — `create()` теперь возвращает
  `null` при превышении (оба места, где создаётся отряд — деплой пресетом и ручная сборка через
  CommandScreen — показывают actionbar-предупреждение вместо тихого отказа). Число выбрано под
  клавиши 1-9 HUD'а.
- ✅ **HUD `B`** (`HudKeys.TOGGLE`, перебиндивается через Controls): открывает/закрывает
  небольшую неблокирующую панель слева по центру экрана (`HudClientEvents`, рендер через
  `RenderGuiEvent.Post` — НЕ `Screen`, поэтому WASD/обзор продолжают работать, пока панель
  открыта). Открытие делает короткий round-trip на сервер за свежим списком отрядов
  (`RequestHudPayload` → `OpenHudPayload`), закрытие — чисто клиентское.
- ✅ **Цифры 1-9** (`HudKeys.SLOTS`) переиспользуются дважды: сначала список отрядов (нажатие —
  выбор), затем (тем же диапазоном клавиш) список приказов DEFEND/PATROL/ATTACK/FREE. Выбор
  приказа сразу отправляет `HudOrderPayload` — сервер одним действием и меняет `order`, и ставит
  `objective` по рейкасту текущего направления взгляда игрока (`ModNetwork.onHudOrder`,
  переиспользует `player.pick(220.0, ...)`, как в старом Command GUI) — никакого отдельного шага
  «прицел-клик». После этого HUD возвращается к списку отрядов (не закрывается), можно сразу
  отдать приказ следующему отряду.
- ✅ **Технический риск — коллизия клавиш**: 1-9 в ваниле жёстко привязаны к хотбару. Событие
  `InputEvent.Key` в этой версии NeoForge не отменяемо, поэтому штатно перехватить нельзя.
  Решение — **Mixin** в `KeyMapping.consumeClick()` (`com.sbwnpc.squad.mixin.KeyMappingMixin`,
  конфиг `mixins.sbwnpc.json`, зарегистрирован в `neoforge.mods.toml`): пока HUD открыт,
  отменяет клик ТОЛЬКО у настоящих ванильных хотбар-мэппингов (`options.keyHotbarSlots`,
  сравнение по ссылке на объект, не по коду клавиши — чтобы не погасить наши же `HudKeys.SLOTS`,
  которые намеренно висят на тех же физических клавишах). Это не первый Mixin в дереве
  зависимостей: у SuperbWarfare уже есть `KeymappingMixin.java`, решающий точно такую же задачу
  (перехват 1-9 для выбора места в технике) — паттерн скопирован оттуда, а не изобретён с нуля.
  Полная сборка проходит (Java-файл компилируется, конфиг попадает в джарник), но собственный
  refmap Mixin AP не генерирует — при сверке выяснилось, что у SBW тот же характер (объявленный
  `mixins.superbwarfare.refmap.json` физически отсутствует в их сборке) и это не мешает их
  миксинам работать, так что для этого тулчейна (NeoForge 1.21.1 + ModDevGradle, маппинги
  official/Mojang без SRG-прослойки) это, видимо, норма, а не поломка — но это тот один кусок,
  который проверяется только реальным запуском игры, статически до конца не подтвердить.
- ✅ **Фикс креша при `B`**: `StreamCodec.unit(value)` требует кодировать КАЖДЫЙ РАЗ тот же самый
  инстанс — `RequestHudPayload()` пересоздавался при каждом тоггле и падал с
  `IllegalStateException: Can't encode ... expected ...`. `RequestHudPayload` теперь `object`
  (синглтон), не `class`. Не имеет отношения к Mixin — крашилось на кодировании пакета, до того,
  как вообще доходило до HUD/Mixin-логики.
- ✅ **Маркер точки-цели**: частицы `DustParticleOptions` цвета отряда в месте, куда поставлен
  objective — видны ТОЛЬКО владельцу отряда через игрок-таргетированный
  `ServerLevel.sendParticles(ServerPlayer, ...)`. Подключено централизованно в
  `SquadManager.setObjective()` (теперь принимает `ServerLevel`), поэтому работает одинаково из
  HUD, старого Command GUI (дальний рейкаст) и близкого клика по блоку — единая точка кода, не
  трижды продублировано.
- ✅ **Фикс устаревшего списка**: HUD-панель показывала уже убитые/распущенные отряды, хотя
  старый Command GUI («вьюха монитора» в терминах пользователя) был корректен — оба фетчат список
  свежим при открытии, но `HudOverlayState.pickSlot()` после отправки приказа возвращался к
  списку отрядов, переиспользуя КЭШИРОВАННЫЙ `rows` с момента открытия, без повторного запроса —
  если отряд погибал/распускался, пока HUD оставался открытым (типичный сценарий — командуешь
  несколькими отрядами подряд, не закрывая панель), список не обновлялся до полного закрытия и
  повторного открытия. Теперь после каждого приказа дополнительно шлётся `RequestHudPayload` —
  список освежается прямо в открытой сессии, без мигания пустотой (старые строки остаются, пока
  не придёт ответ).
- ✅ **Подтверждено пользователем в игре, проблем не замечено**: визуал панели, Mixin-перехват
  хотбара (цифры 1-9 больше не листают хотбар, пока HUD открыт), цепочка order+objective за одно
  нажатие, частичный маркер, обновление списка отрядов — всё работает как задумано.

## Мультиплеер: права владения 🚧 (первый проход)

Пользователь тестирует через друзей по Tailscale/headscale-туннелю; всплыл вопрос прав доступа
между разными игроками на одном сервере.

- ✅ **Проверка владения отрядом на сервере**: раньше ЛЮБОЙ игрок мог ПКМ по чужому NPC в
  Command-режиме — это выбирало чужой отряд, дальше можно было менять приказ/objective/focus,
  переименовать или распустить его. Ни один из `SquadCmdPayload`-обработчиков не проверял
  `squad.owner == отправитель`. Добавлено:
  - `SquadManager.ownedBy(id, player)` — единая проверка.
  - `ModNetwork.onSquadCmd`: `ownedSid()` (замена старого `sid()`) проверяет владение перед
    DISBAND/SET_ORDER/RENAME/SELECT/ARM_OBJECTIVE/ARM_FOCUS, иначе actionbar «Not your squad».
  - `ModNetwork.onHudOrder`: та же проверка перед SET_ORDER+objective из HUD.
  - `SquadToolItem.interactLivingEntity`: клик по чужому засквоженному NPC больше не выбирает
    чужой отряд («Not your squad» вместо тихого succeess). ARM_FOCUS-ветка ownership уже
    проверила на этапе взвода (`armFocus` идёт через `ownedSid()`), поэтому доп. проверки не
    нужно.
  - Все downstream-чтения `SquadSelection.selectedSquad(...)` (близкий клик objective, focus на
    не-NPC цель) теперь транзитивно безопасны — состояние выборки просто не может указывать на
    чужой отряд, раз все точки входа в него проверены.
- ⬜ **Не закрыто**: «кража» ещё не сгруппированного NPC. `NpcEntity` не хранит, кто его
  задеплоил/рекрутировал (только `squadId`, который `null` до формирования отряда) — до того, как
  отряд сформирован, ЛЮБОЙ игрок технически может toggle-select чужого свежезадеплоенного NPC в
  свою loose-выборку и забрать его себе через «Create». Узкое окно (обычно деплой → формирование
  почти сразу), но не защищено. Нужно поле owner на `NpcEntity`, если станет реальной проблемой.
- ⬜ **Отложено пользователем осознанно** (не делаю сейчас): фракция отряда сейчас общая на
  сервер — vanilla-команда шарится по названию фракции, а не привязана к игроку+фракции, так что
  два игрока, выбравшие одну и ту же фракцию (например, оба Piggies), автоматически станут
  союзниками. Цвет заменён на фракцию (см. ниже), но структурная природа проблемы та же. На время
  разработки оставляем как есть (свободный выбор любой фракции всем). План на будущее (с
  формулировки пользователя): каждый игрок при первом взаимодействии с GUI получит закреплённую
  «свою» фракцию по умолчанию; админ сервера сможет через чат-команду выдавать конкретному игроку
  право спавнить/выбирать ЛЮБУЮ фракцию (в обход дефолтного закрепления). Не реализовано — только
  зафиксировано как решение.

## Фракции вместо цвета ✅ (подтверждено пользователем в игре)

Идея пользователя: различать отряды не оттенком, а силуэтом/скином — заметнее в бою, чем тонкая
разница в цвете ника, плюс приятная тема («свин в военной форме» и другие звери-гуманоиды).
Пользователь скинул 8 готовых текстур (`cat/cow/creeper/ender/panda/pig/sheep/wither`, 64×64,
под уже используемый `HumanoidModel`-layout) и переименовал старый `npc.png`.

- ✅ **`SquadFaction`** (новый enum, `com.sbwnpc.squad.npc`) — 8 фракций, каждая несёт `label`
  (для GUI: Piggies/Cows/Creeps/Sheep/Pandas/Cats/Enders/Withers), `texture` (путь строится из
  имени enum-константы) и `accentColor` — тот же набор из 8 `ChatFormatting`, что был раньше,
  теперь произвольно закреплён за фракциями (не претендует на "реалистичное" сопоставление
  цвет↔животное, только читаемость текста в чате/UI).
- ✅ **`SquadTeams` теперь ключуется по `SquadFaction`**, не по `ChatFormatting`: имя vanilla-
  команды — `sbwnpc_<фракция>`, `factionOf(entity)` разбирает его обратно из имени команды
  (никакого доп. synced-поля не понадобилось — членство в scoreboard-команде и так синкается
  клиенту сама по себе). `isHostile`/`assign`/`clear` не поменяли поведение, только тип ключа.
- ✅ **`NpcRenderer.getTextureLocation()`** теперь реально использует переданный `entity` (раньше
  игнорировал параметр) — возвращает `SquadTeams.factionOf(entity)?.texture`, с фолбэком на
  `SquadFaction.DEFAULT` (Piggies) для нейтральных/безфракционных NPC.
- ✅ Переименования по всей цепочке: `NpcEntity.spawnColor` → `spawnFaction`; `Squad.color` →
  `Squad.faction` (+миграция NBT-поля `Color`→`Faction`, старые сохранённые отряды до этой правки
  не мигрируют автоматически — если завалится тест, точка для проверки); `SquadToolItem.Config` и
  `ConfigureToolPayload` — `color`→`faction`; снапшоты для GUI/HUD (`buildSquadSnapshot`,
  `CommandScreen`, `HudOverlayState`) читают/пишут `"Faction"` вместо `"Color"`.
- ✅ **Подтверждено пользователем в игре, проблем не замечено**: скины рендерятся корректно
  (без наложения на слои брони/руки), смена фракции в Recruit GUI работает, отображение
  корректно везде (Command GUI, HUD).

## Фаза 5 — Тактика, фракции игроков, логистика 🚧 (все пункты собраны, ждут теста в игре)

Набор запросов пользователя после аудита Фаз 1-4.5: HUD-приказ сразу всем своим отрядам
(клавиша `0`), обязательный (и закреплённый) выбор фракции игроком при первом
взаимодействии с тулом, избегание союзников на линии огня, укрытия/подавление (MVP),
слух/тревога отряда (и привязка миномёта к реально обнаруженным целям вместо слепого
скана по радиусу), казарма как разрушаемый плейсбл-спавнер, waypoints для патруля через
отдельную вьюху маршрутов. Подробный план с алгоритмами и обоснованиями — в
`PHASE5_PLAN.md` в корне репо (сознательно НЕ в git — рабочий черновик, не история
проекта). Здесь, в PLAN.md, фиксируется только факт входа в фазу и краткая сводка;
детальный статус по каждому пункту — в том файле.

- 🚧 HUD: клавиша `0` — приказ сразу всем отрядам под командованием игрока СВОЕЙ
  фракции (фильтр по `PlayerFactionRegistry`-дефолту — тестовый отряд другой фракции
  под тем же владельцем не трогает). Собрано, задеплоено, ждёт теста в игре.
- 🚧 Обязательный (один раз) экран выбора фракции при первом взаимодействии с тулом —
  фиксирует дефолт на будущее. **Свободный выбор любой фракции на деплое/в Recruit GUI
  остаётся полностью рабочим** — по прямой просьбе пользователя это осознанно оставлено
  на время разработки, реестр сейчас не замок, а только запись дефолта. (Была ошибка в
  первой версии — сервер игнорировал выбор клиента и всегда подставлял закреплённое
  значение; исправлено по фидбеку.) Собрано, задеплоено, ждёт теста в игре.
- 🚧 ИИ не стреляет по своим на линии огня (все стрелковые классы, включая гренадира).
  Первый проход не работал — проверялась идеальная линия нулевой ширины, а реальный
  выстрел летит с разбросом (`spread`); плюс не было отдельной проверки радиуса
  поражения для взрывного оружия (M79/граната). Второй проход: честный конус разброса
  + независимая проверка блaст-радиуса у цели, пересчёт каждый тик. Собрано,
  задеплоено, ждёт теста в игре.
- 🚧 Укрытия / подавление — полное поведение (не MVP): цикл
  `MOVING_TO_COVER → IN_COVER → PEEKING → RETURNING_TO_COVER`, во время `PEEKING` боец
  сам выходит и отстреливается, потом снова прячется, пока подавление активно. Первый
  проход не срабатывал вообще — `hurt()` проверял ванильный тег урона, которым SBW не
  метит свою стрельбу; исправлено на реальный тег SBW. Собрано, задеплоено, ждёт теста.
- 🚧 Слух / тревога: общая осведомлённость ФРАКЦИИ, наводчик миномёта — сам спотер.
  Первый проход не работал — `SquadFocusTargetGoal` проставлял `mob.target` наводчику
  при любом командном приказе, из-за чего `MortarOperatorGoal` принимал это за угрозу
  себе и выходил из боевого цикла раньше, чем доходил до скана/чтения осведомлённости.
  Исправлено — эти два класса больше не получают squad-focus таргет. PM-ревью нашёл
  остаточный пробел (тот же бэйлаут срабатывал и от общих `HurtByTargetGoal`/
  `NearestAttackableTargetGoal`, не только от squad-focus) — тоже исправлено: бэйлаут
  теперь только на реально близкую угрозу (≤6 бл), а не на любой `mob.target`. После
  теста игрок сообщил, что пехота всё равно не реагирует на чужое обнаружение —
  оказалось, сторона "потребления" `TeamAwareness.relayedContacts` была подключена
  ТОЛЬКО к миномёту, обычная пехота вообще её не читала. Добавлен
  `SquadAwarenessTargetGoal` (targetSelector, приоритет 3) — читает её и для пехоты
  тоже. Раунд 2: пользователь верно указал, что это всё ещё не "ощущается" —
  почитал реальные инварианты шутерного AI (F.E.A.R./Half-Life: слуховой стимул,
  тревога отряда при потере союзника) и добавил `Alarm` (услышанная рядом стрельба
  союзника → сосед получает `alert()`, не таргет) + `die()` теперь либо репортит
  убийцу в `TeamAwareness` напрямую (если резолвится), либо поднимает `Alarm` на
  месте смерти + новый `InvestigateGoal` (приоритет 4), который реально идёт
  проверить тревогу. Собрано, НЕ протестировано в игре.
- 🚧 Казарма — теперь настоящий БЛОК (`BarracksBlock`/`BarracksBlockEntity`), не Mob —
  изначальный Mob-вариант не совпадал ни со словами пользователя, ни с собственными
  конвенциями SBW (все функциональные плейсблы SBW — обычные блоки без тула). ПКМ
  голой рукой = привязка отряда, без мультитула. Разрушается только киркой/взрывом
  (сознательно принятое изменение — очередь из автомата больше не убивает казарму).
  `Squad.barracksId: UUID?` → `Squad.barracksPos: BlockPos?`. Раунд 2: реальный баг —
  предмет был зарегистрирован, но не добавлен ни в одну вкладку креатива
  (`ModCreativeTab.kt`), физически недостижим в игре. Исправлено (FUNCTIONAL_BLOCKS).
  Собрано, НЕ протестировано в игре.
- 🚧 Waypoints — вьюха маршрутов (кнопка Routes в Command GUI, `+ Add route` сверху
  списка), режим записи точек кликами (независим от режима тула, как arm-фокус/
  objective), назначение маршрута отряду для PATROL — идёт по точкам последовательно
  с паузой, без маршрута — старое случайное блуждание. Собрано, задеплоено, ждёт
  теста в игре.
- 🚧 Формации отряда (новый пункт, не было в исходном плане Фазы 5 — добавлен по
  фидбеку после теста): `SquadFormation` — клин на ATTACK, линия на DEFEND, колонна на
  PATROL, у каждого члена свой слот-офсет вместо общей точки (чинит и "толпу в одну
  точку", и "змейку"); при достижении позиции — кольцо-периметр, взгляд наружу, вместо
  застывшего марширующего строя. Подключено в `SquadOrderGoal`. НЕ применено к
  активному боевому approach (`NpcGunAttackGoal`) — отдельный случай, не трогал.
  Раунд 2: пользователь верно заметил, что построения не похожи на форму вообще —
  реальный баг: каждый член отряда считал СВОЙ вектор поворота от своей позиции,
  из-за чего ориентация формации была несогласованной между членами (выглядело как
  шум). Исправлено — единый разворот от позиции лидера (слот 0) на весь отряд.
  Проверено субагентом ручной трассировкой на конкретном примере. Собрано, НЕ
  протестировано в игре.
- 🚧 Укрытия — после теста выяснилось, что видимое "отошёл и встал" вероятнее всего было
  `FriendlyFireGuard.sidestepAwayFromAllies`, а не `SeekCoverGoal`. Временные логи
  (`[cover-debug]`) добавлены на `suppress()`/`coverPhase`, чтобы следующий тест дал
  факт, а не догадку — реальный фикс отложен до этого. Раунд 2 также нашёл и исправил
  реальный баг в самом sidestep: `MAX_SIDESTEP_ATTEMPTS` останавливал попытки НАВСЕГДА
  после 3 неудач вместо периодического повтора — именно это выглядело как "стоит
  сзади, не отходит".
- 🚧 Дальность обнаружения vs дальность стрельбы (новый пункт, добавлен по фидбеку
  раунда 2) — разница была технически (`FOLLOW_RANGE`=48+LOS для обнаружения,
  `shootDistance`≈24 для боя), но не была явной механикой, и сближение шло сплошным
  спринтом, а не перебежками. Добавлен bounding-advance в `NpcGunAttackGoal`
  (рывок~1.25с/пауза~1с до shootDistance). Субагент нашёл реальный баг в первой
  версии (off-by-one — первый рывок всегда стартовал с ложной паузы вместо движения);
  исправлено. Собрано, НЕ протестировано в игре.
- ✅ Производительность — расследовано с реальными данными (spark, после починки
  краша JDK26↔async-profiler и повторного профиля с `--thread *`). Наш код
  (рендер НПС, боевые системы) — не причина, копейки на Render thread (0.4-0.5%).
  Проверено ЭКСПЕРИМЕНТАЛЬНО: без Distant Horizons просадка FPS осталась —
  пользователь подтвердил, что дело в Sodium/Iris (шейдерпак), не в DH и не в
  sbwnpc. Вопрос закрыт, дальше не трогаю — это тюнинг клиента. Подробности в
  PHASE5_PLAN.md.
- 🚧 Раунд 3 (после теста раунда 2, 7 пунктов — детали в PHASE5_PLAN.md): реальный
  баг-цикл "кольцо тикает" у ATTACK-объектива (порог прибытия был меньше RING_RADIUS),
  формации распространены и на боевой approach в `NpcGunAttackGoal` (снейк при
  обнаружении без явного ATTACK), `Alarm` теперь ведёт к позиции ЦЕЛИ, а не стрелка,
  дальность обнаружения x1.5 (`FOLLOW_RANGE`/`DEATH_ALARM_RADIUS`/
  `GUNFIRE_HEARING_RADIUS`), казарма чинит спавн по фракции ОТРЯДА, а не владельца.
  Перепроверено вторым субагентом (только перечисленные файлы) — всё подтверждено.
  Собрано, задеплоено, НЕ протестировано в игре.
- 🚧 Раунд 4: НПС теперь открывают двери (`OpenDoorGoal` + `setCanOpenDoors(true)` —
  оба пункта нужны, ванильный `DoorInteractGoal` жёстко проверяет второе); спавн
  отряда (деплой линией, респаун в бараке) теперь ищет безопасный Y
  (`SafeSpawn.findSafeY`) вместо фиксированного — не спавнятся внутри блоков.
  Перепроверено субагентом. Собрано, задеплоено, НЕ протестировано в игре.

## Фаза 6 — Техника и дроны ⬜

- Менеджмент-тул: наведение на технику/дрон → присвоение цвета отряда.
- Доработка friend/foe `TowerAI.TeamResolver` для автономной техники (резолв по
  `entity.team` без `tower.owner`).
- Визуальная индикация цвета (в духе `VehicleTeamOverlay`).

## Фаза 7 — Получение NPC ⬜

- Основной способ найма — режим «Вербовка» тула (см. Фазу 3), по духу `AbstractDeployerItem`.

## Фаза 8 — Полировка ⬜

- Лут с убитых NPC (оружие/патроны/броня).
- Барки/озвучка (вижу цель, перезарядка, отступление) — через звуковую систему SBW.
- Анимация отдачи при выстреле.
- Балансировка через конфиги (Cloth Config).
